import {headerLogo} from '../assets/images'

const Nav = () => {
  return (
    <header>
        <nav>
            <a href="/"/>
            <img
            src = {headerLogo}
        />
            </nav>
    </header>
  )
}

export default Nav;